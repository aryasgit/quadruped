# BARQ v1 stack — install into the workspace venv ONLY:
#   ~/barq_v1/venv/bin/pip install -r stack/requirements.txt
flask>=3.0
smbus2>=0.4
PyYAML>=6.0
xacro>=1.14
numpy>=1.24
pybullet>=3.2     # builds from source on aarch64 (~minutes, needs python3-dev)
pytest>=8.0
